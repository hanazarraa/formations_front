import { Enregistrement.Model } from './enregistrement.model';

describe('Enregistrement.Model', () => {
  it('should create an instance', () => {
    expect(new Enregistrement.Model()).toBeTruthy();
  });
});
